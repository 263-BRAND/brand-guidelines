# 263 VI PPT 模板系统 — 项目进度

**更新：2026-08-18 (交互措辞重构：用户话术唯一输出源 + 关键词冲突规则 + 内部汇报→工作汇报)**

---

## 2026-08-18 交互措辞重构：正向白名单 + 用户术语「工作汇报」

### 根因
- 跨工具实测：生成前交互仍暴露内部技术词——工具 agent 在「模式判断」直接输出「工作汇报→命中硬锚词→Template（内部汇报）」，在「硬门禁确认」把「硬门禁」当步骤名说给用户
- 工具 agent 反馈：这些词来自 SKILL.md 规范，工作流步骤名（硬锚词/Template/硬门禁确认）是它逐字照抄流程的结果
- 本质：SKILL.md 用内部术语写工作流叙事，负向「对话黑名单」压不住——黑名单把禁词全列出来反而强化激活（writing-for-agents：negation 是失效模式，应正面陈述目标行为）

### 修法（SKILL.md 交互章节三层重构）
- **① 场景判断章节 →「场景判断（Agent 内部逻辑）」**：顶部显式标注"本节是内部思考用，不进入对用户的话"；关键词判定表改用用户安全词（判定为：工作汇报/对外展示），去掉「硬锚词（命中即锁定）」叙事与「→ 命中 → Template」判断链示例
- **② 关键词冲突规则**：用户资料同时含内部与外部关键词（如「为客户提供服务周报」含「客户」+「周报」）→ 不自动锁定，以第 1 问用户确认为准。覆盖服务部门定期给客户交付服务周报的场景（形式是工作汇报、受众是对外，判定应跟随受众/应用场景）
- **③ 生成前确认章节重排，正向白名单置首**：「用户话术（对用户的唯一输出）」提到最前——"对用户说的每一句话只能逐字来自下方编号话术"；原负向「对话黑名单」降级为「内部思考词汇」对照表（供内部翻译，非输出检查点）；三条铁律改为正向（逐字话术/不播报判断过程/不暴露内部信息）
- **④ 工作流步骤改名用户安全词**：步骤 1「判断模式」→「判断场景」；步骤 2「硬门禁确认」→「生成前确认」；透明声明标签 Template/Themed → 工作汇报/对外展示
- **⑤ 用户术语调整**：「内部汇报」→「工作汇报」（对外保持「对外展示」）——SKILL.md 全部用户话术、对照表、章节标题（工作汇报红色封面）统一

### 工作区恢复（toc 渲染器）
- 发现工作区 generate.js/section.js/brand-tokens.json 被旧版本覆盖（无 toc、无行距 token），与提交态/开发日志/0817 zip 矛盾；来源是外部进程（非 git 操作、非本会话改动、无 hook）。已 `git restore` 恢复提交态（含 toc + 行距），剩余 M 仅为 CRLF/LF 行尾噪音（core.autocrlf=true），`--ignore-space-at-eol` 下零 diff

### 同步
- CLAUDE.md（对话术语 bullet：补唯一输出源说明 + 三铁律 + 引用路径改「生成前确认 → 内部思考词汇」；硬门禁 bullet、封面模式表）+ Design-Decision.md（前置步骤第 2 步 + 引用路径）
- 重建 `测试记录/263-vi-skill-0818.zip`（18 文件，含 toc.js；一致性自检通过：toc/行距/工作汇报术语/冲突规则都在）

---

## 2026-08-17（追加3）目录页 toc 类型 + 行距规范覆盖章节页/目录页

### 根因
- Trae 实测：行距规范漏掉章节页（section）与目录页；且系统无 toc 渲染器，目录页只能用 content 拼凑

### 修法
- **新增 `renderer/slides/toc.js`**：slide 字段 `title` + `items[]`（每项 `index` + `text`），对齐 content 版式骨架（白底/右上 Logo/top:12% 标题/28% 内容区/底部页码）；`generate.js` slideTypes 加 `toc` → 共八种类型
- **行距表补齐**：章节页标题 1.3/单倍、章节页副标题 1.8/1.2倍、目录页标题 1.3/单倍、目录条目 `tocItem` 1.8/1.2倍
- **章节页大号数字行距** `sectionNumber`：HTML 2.0 / PPTX 1.2倍——64pt 大号数字原 1.3 行距盒高过小、数字与标题贴近；2.0 拉开垂直空间视觉透气。字号仍复用封面标题 64pt（未解耦，用户未要求）
- section.js 行距从硬编码/margin 改为读 token

### Code review / 实测
- 浏览器实测通过：toc 目录项、章节页带/不带副标题、大号数字行距 2.0、窗口缩放 1280×800、翻页遍历（End/方向键）、封面 red-template
- 后台 agent 确认大号数字字号复用 coverTitle 64pt（非独立 token），行距独立 sectionNumber

### 同步
- CLAUDE.md（行距 bullet、核心文件表、slide 类型） + Design-Decision.md（行距扩展决策记录） + SKILL.md（行距表、pages.json 结构）
- 重建 `测试记录/263-vi-skill-0817.zip`（18 文件，含 toc.js）

---

## 2026-08-17（追加2）封面风格推荐与输出格式联动（双向强制）

### 根因
- Trae 重测：用户已选「网页文件」做内部汇报，Agent 仍推荐严谨商务风格——推荐逻辑错误
- 原因：Q5 推荐条件写成「输出格式为 PPT 时补充推荐」的描述性话术，Agent 未联动 Q4 答案，无条件补推荐

### 修法（SKILL.md Q5 双向强制）
- **PPT 文件 → 必须推荐**严谨商务风格（个性化像素动效在 PPT 无法原生渲染，严谨是 PPT 适配封面）
- **网页文件 → 禁止推荐**严谨风格（网页两个风格都原生支持，用户自选，不得附加推荐语）
- 硬规则补一条：封面推荐必须与 Q4 联动，先问完 Q4 才能决定 Q5 是否补推荐；违反=交互逻辑错误

### 同步
- CLAUDE.md 硬门禁 bullet + Design-Decision.md 前置步骤第 2 步：补联动约束
- 重建 `测试记录/263-vi-skill-0817.zip`

---

## 2026-08-17（追加）交互措辞三铁律：对话黑名单

### 根因
- Trae 重测：文档标题含「汇报」+ 标注「汇报部门/汇报时间」→ 硬锚词正确命中、正确锁定内部汇报，**但交互环节仍把内部词汇暴露给用户**（内部术语 Template/母版/锚词/命中/锁定 + 内部文件名 red-template/pages.json/scene/background/brand-tokens.json 等）
- 原因：原规范是"负向一条禁止"，埋在长禁止事项里；跨工具 agent 一致性差，临场发挥时把判断依据和内部名说出来

### 修法（SKILL.md 硬门禁升级为三件套）
- **① 逐字话术**：确认问题必须逐字原样使用固定用户话术，禁止自行发挥/改写/加解释
- **② 禁止播报判断依据**：命中/锚词/锁定/检测到等推理词不出现，判断过程只留内部、对用户只输出结果话术
- **③ 对话黑名单表**：内部说法 → 用户话术替换对照（Template→内部汇报/对外展示；red-template→严谨商务风格；pages.json/scene/background/brand-tokens.json 等不出现；ASCII→个性化风格）
- 双保险：禁止事项条目引用黑名单位置（见「生成前确认 → 用户对话措辞规范 → 对话黑名单」）

### 同步
- CLAUDE.md 对话术语 bullet + Design-Decision.md 前置步骤第 2 步：补三铁律与黑名单引用
- 重建 `测试记录/263-vi-skill-0817.zip`

---

## 2026-08-17 进展：图表配色规范 v2 + 严谨封面排版 + 交互规范

### 图表配色规范 v2（chartPalette）
- 根因：Trae Work 测试 PPTX 时 agent 无法执行图表颜色要求，原 4 色红系视觉区分度太低，agent 自造色
- `brand-tokens.json` v2.2 → v2.3：新增 `chartPalette`——7 档红系色阶（`#F8B5B8`→`#72090E`）+ 非重点静默色（浅粉 `#FF777F` / 淡灰 `#BFBFBF`）
- 上色规则（SKILL.md 唯一真相源，JSON 只存色值）：**模式① 重点突出**（1~2 重点=品牌红，其余=浅粉/淡灰，agent 自动判断）> **模式② 色阶方向**（按用户顺序铺色阶，第一端=最浅或最深，必须二选一）；二次编码（折线线型+标记 / 柱状数值标签 / 饼环白分割线+标签 ≤7 扇区）
- dataviz 验证：纯红系相邻 ΔE 仅 10.9 不达标；红+蓝合并可过但被否（偏离色系）；最终人类肉眼验证采纳色阶 + 重点规则
- 排除玫红/绛紫（偏离主色色系）；商务蓝色板待官方确认

### 严谨封面（red-template）排版
- 左上角集团红心 Logo（= 集团彩稿 `logos.group.color`，与内页一致），位置固定 6%/8%/125px（PPTX 62pt，坐标 57.6/43.2pt）
- 主标题 ≤20 字符/行不折行，超限改短或分行（`\n`）；宽度自适应上限 50%
- 副标题独立行 30pt 非粗 > 汇报信息 26pt；间距分层 标题→副标题 16px / 副标题→汇报 32px
- 文字块垂直居中 `startY = (SH − totalH) / 2`（HTML height:100% flex）；公司全称 bottom:6% 不动
- 浏览器实测通过（缩放/翻页/背景模式/字号/间距/位置）

### 行距规范（HTML/PPTX 两套，2026-08-17）
- Trae 实测发现行距偏小（渲染器硬编码 1.6）。Template 硬规则三项（字号/颜色/Logo 位置）不含行距，故行距改为品牌定值
- **HTML（CSS line-height）**：主标题 1.3、小标题/正文/卡片/时间轴 1.8（标题 1.3 避免撞内容区，之前 2.0 造成标题与小标题重叠）
- **PPTX（倍距）**：标题单倍(100%)、其余 1.2倍(120%)——PPTX 与 HTML 是两套规范，PPTX 1.2倍 ≈ HTML ~1.6，禁止互套
- 行距写入 `brand-tokens.json` → `typography.lineHeight`（html/pptx 两套），渲染器从 token 读取（消除硬编码）
- 三页标题位置统一 top:12%（content 原 18%，cards/timeline 12%，已统一）

### 交互规范（2026-08-17，Trae 实测反馈后重做）
- 禁止向用户暴露内部文件名/字段值（red-template、scene、background 等）——对话术语扩展 + 禁止事项
- **硬门禁按场景分支**：内部汇报 → Q1/Q2/Q4/Q5；对外展示 → Q1/Q2/Q3/Q4
- Q1 改「先判断 + 确认式提问」（判断是 A 则问是否需要改成 B），不再开放二选一
- Q3（视觉风格）仅对外展示询问，行为有定义：严格=母版锁定 / 美化=设计 skill 自由；内部默认严格按模板
- Q5 封面风格：个性化风格（Geek 像素动效） vs 严谨商务风格；如生成 PPTX 推荐严谨商务风格
- **确认硬性必问**：Agent 判断不能替代用户确认，禁止因已判断而跳过
- 固定用户话术，禁止自行发挥、禁止向用户解释母版/硬规则等内部机制

### Code review
- 两次 review（图表色阶 + 封面）：修复 SKILL 规则冲突、红心 logo 去重复用、PPTX 坐标补全、无副标题间距回退、魔法数字读 token、抽 coverLogoBlock 共用函数、悬空引用、白名单措辞

### Skill zip
- 重建 `测试记录/263-vi-skill-0817.zip`（17 文件，自包含：SKILL.md + assets + renderer + brand-tokens + company-data + generate.js），zip 从**根目录**按 17 文件清单构建
- `visual_skill/` 与 `visual_skill_v2/` 遗留部署包已删除（冗余、易漂移，2026-08-17）

### 待办
- [ ] **跨工具 PPTX 行距实测**——测试提示词已备（`测试记录/测试提示词-pptx行距.md`），覆盖封面/目录/章节/正文/卡片/时间轴全部行距场景。判定：大部分工具设对→靠自觉；个别设对→加清单式硬规定；没人设对→直接上硬规定
- [ ] **跨工具复测交互措辞**——新 zip（0818）生成前交互是否不再暴露内部技术词（命中/锚词/Template/硬门禁等），确认问题是否逐字用「用户话术」；关键词冲突用例（如「为客户提供服务周报」）是否不自动锁定、以用户确认为准
- [ ] Trae Work 实测新 zip（图表配色 + 严谨封面 + toc 目录页 + 交互三铁律 + 封面推荐联动）
- [ ] 商务蓝图表色板（官方确认后按同法定义）

---

## 2026-08-14 进展：内部汇报红色位图封面（red-template）

### 需求
- 用户自制封面图片 `视觉参考/background-red-template_pc.png`（4608×2592），作为 ASCII 字符画之外的第二种内部汇报封面

### 实现
- 图片等比缩放到 `assets/cover-red-template.png`（1920×1080，898KB）
- `brand-tokens.json`：新增 `redTemplateCover` 字段（`path` + `note`）；版本号保持 v2.2（数据层扩展，非破坏性）
- `renderer/slides/cover.js`：新增 `renderRedTemplate` 分支——全屏位图底图 + 左对齐文字（标题 dark 64pt、副标题/汇报人 gray 30pt 红色圆点分隔、公司全称 bottom:6% 浅粉带上）
- `generate.js`：读取 `cover-red-template.png` 内嵌 base64 → `.red-template-bg` CSS 类（`center/contain`）
- 触发：`scene: "template"` + cover `"background": "red-template"`；不设置 `background` 仍为 ASCII 封面

### 封面风格交互（硬门禁第 5 题）
- 触发方式从「技术字段」升级为「用户选择」：内部汇报场景追加第 5 题「封面风格」——个性化 → ASCII 字符画，严谨 → red-template
- PPTX 输出时推荐严谨（ASCII/二进制雨无法原生渲染，严谨位图封面 HTML/PPTX 100% 一致）
- 映射写入 pages.json：严谨 → 封面 `"background": "red-template"`，个性化 → 省略 `background`

### 验证
- 浏览器实测：背景图加载（contain）、标题色 `#2D3847` 64pt 左对齐、meta gray + 红色圆点分隔、翻页到结尾页正常
- 回归：默认 ASCII 封面（38 ascii-line + binaryRain）不受影响；Themed 红底渐变封面不受影响

### Code review 修复
- `background-size:cover` → `contain`（对齐「禁止裁切」硬门禁，16:9 图与画布同比例结果不变）
- 删除 `backgrounds.cover["red-template"]` 死入口（bitmap 哨兵值），触发改为 cover.js 分派 + generate.js 校验特例 `red-template`
- `redTemplateCover` 精简为 `path` + `note`，删除会漂移的 `scene`/`trigger`/`layout` 散文（真实布局在 cover.js + SKILL.md）
- **body 底色统一 `#FFFFFF`**：SKILL.md 母版封面页曾写 `#F2F2F2`（lightGray），与代码/Design-Decision/封面双模式章节冲突。统一为纯白——red-template 位图白色区（253~255）与纯白无缝，`#F2F2F2` 仅保留作图表结构底色（表头/斑马纹）。渲染器保留字面量色值（不引入 `c.white` 变量，跨工具可读性最稳）。

### 待办
- [x] 重建 skill zip（含 `cover-red-template.png`，17 文件）
- [ ] PPTX 路径实测（全幻灯片背景 + 文字框叠加 + FillVisible=false）——待用户测

---

## 2026-08-12 进展（下午）：PPTX 字号修正 + 封面设计方向

### PPTX 封面标题字号修正
- 根因：CSS pt 在 1920×1080 画布 ≈ 1.333px/pt，PPTX pt 在 960×540 幻灯片 = 1pt/pt。直接复刻 HTML 64pt 到 PPTX 导致比例不对（HTML 7.9% vs PPTX 11.9%）
- 修复：PPTX 封面标题 64pt → 43pt，与 HTML 视觉比例对齐

### PPTX 封面文字框透明
- 新增 MUST：PPTX 封面文字框必须 FillVisible=false，透明背景，禁止填充颜色
- 禁止事项同步更新

### 封面设计方向探索
- 严谨风格（Formal）：浅灰底 + 顶部红色通栏 + 左上彩稿 Logo + 居中排版，HTML/PPTX 零差异
- 位图底图方向：豆包生成封面位图 → HTML/PPTX 共用底图 + 动态文字叠加，100% 视觉一致

---

## 2026-08-12 进展（上午）：PPTX 输出路径补齐 + 图片素材缩放硬门禁

### 问题
1. 用 AI 工具测试发现：Logo/slogan 图片被裁切变形，Agent 不引用图片素材
2. SKILL.md 整个生成管线为 HTML 设计，PPTX 路径缺失关键定义（画布尺寸、图片嵌入、图表规范）
3. 多处数据路径引用 JSON 中不存在的 key（`typography.html.scale`、`logos.businessLines`）

### 修复

**图片裁切/变形：**
- `cover.js`：Themed 封面 Logo 外层 div 无尺寸 → 0×0 不可见；`6.5%` 在 1920×1080 上解析为 125×70 矩形 → 改用 px（125×125）
- `end.js`：Logo 288×162 (16:9) → 356×356（33% 画布高）；Slogan `height:80px` → `height:auto`
- SKILL.md 新增「图片素材缩放规则」硬门禁章节：5 条铁律 + 场景正确/错误写法 + 自检清单，HTML/PPTX 双格式覆盖
- brand-tokens.json hardRules 新增 noCrop/noDistort/embedRequired

**PPTX 路径补齐：**
- PPTX 画布 960×540pt 写入 brand-tokens.json → layout.pptx + SKILL.md 布局章节
- 所有尺寸示例改为双格式对照（HTML px / PPTX pt）
- Template 封面 PPTX 回退：`assets/template-cover-bg.png`（ASCII 字符画静态截图）作为全幻灯片背景，文字框固定位置叠加（标题 top:48%、副标题 top:62%、公司 top:82%，与 HTML 一致）
- PPTX 图片嵌入规范：内嵌二进制 + 禁外部链接 + LockAspectRatio 正确顺序
- 数据图表每种类型增加 PPTX 原生图表对照说明

**陈旧数据路径修复：**
- `typography.html.scale` / `typography.pptx.scale` → `typography.sizes`
- `logos.businessLines` → `logos.group` / `logos.cloud`
- brand-tokens.json v2.1 → v2.2

**HTML-only 规则标注：**
- 响应式缩放、二进制雨、ASCII 动画、custom slide、base64 嵌入、HTML 输出规范 —— 7 处标注「仅 HTML」

**字体回退链：**
- 微软雅黑 → Noto Sans SC → Source Han Sans SC → sans-serif（开源无版权，跨平台兼容）

### Skill zip
- 重建 `测试记录/263-vi-skill-0812.zip`（16 文件，新增 template-cover-bg.png）

---

## 2026-08-11 进展（跨工具兼容性修复）：模式判断 + 确认门禁强化

### 问题
另一个 AI 工具加载 VI skill 后，对"帮我做个Q3工作汇报ppt"：
- 未弹出「内部汇报/对外展示」确认对话框
- 封面错误地使用了 Themed 红底渐变+左上 Logo，而非 Template 白色+ASCII

### 根因
SKILL.md 三个设计缺陷：
1. **模式判断说"不穷举关键词"** — 导致 agent 跳过"汇报 → Template"的直接映射
2. **确认环节不在工作流编号中** — agent 顺着 1→2→3 编号走，确认被跳过
3. **确认结果没强制映射 pages.json** — 「内部汇报」选了但 `scene` 字段未设，渲染器默认 Themed

### 修复

| # | 改什么 | 效果 |
|---|--------|------|
| 1 | 模式判断增加硬锚词 | "汇报/述职/总结/周报/月报/季报/年报" → Template 直接锁定，跳过推理 |
| 2 | 确认升级为硬门禁 + 编号步骤 | 工作流第 2 步，自检语句"如果没问完就停下来" |
| 3 | 确认答案强制映射 pages.json | 「内部汇报」→ `"scene": "template"`，不可省略 |

### Skill zip
- 重建 `263-vi-skill-20260811.zip`（15 文件，112KB），目录结构完整，无 flatten 重复

---

## 2026-08-11 进展（最终）：交互优化 + 渲染修复

---

## 2026-08-11 进展（最终）：交互优化 + 渲染修复

### 用户交互优化
- 生成前确认 4 问（场景/配色/视觉/格式），确认前不做透明声明
- 前置步骤新增大纲收集 + 数据图表收集
- 对话去术语化：禁止 Template/Themed/统一母版等内部词汇
- 透明声明话术去括号、明确时间点

### Skill zip 自包含化
- 补全缺失文件：generate.js、renderer/、assets/
- 新增 pages.json schema 章节
- 明确 ASCII CSS transform 和 Logo base64 嵌入规则
- 删除 Design-Decision.md 引用（项目内部文档，非 skill 必需）

### 渲染器修复
- custom.js footer 对齐其他渲染器（top:84% → bottom:3%）
- ASCII 位置冲突修正（表 9% → 实际 16%）
- 内容安全区统一：top:12% ~ bottom:18% + 50% 垂直居中规则
- 刷新保持当前页（sessionStorage）+ 切回封面重播 ASCII 动效
- 播放器真垂直居中（top:50%; translate(-50%,-50%)），解决非 16:9 窗口"偏下"问题
- 浏览器标签页标题：封面标题 - 姓名 - MMDD（替换内部色号名）

---

## 2026-08-11 进展（晚间）：数据图表规范完成

### 图表模块完整验证

四种图表类型全部通过测试，规范写入 skill.md 和 SKILL.md：

- **柱状图** — CSS 实现，渐变两端色值必须来自品牌色板
- **折线图** — 单 SVG + 固定 `viewBox="0 0 740 520"`，禁止 CSS/SVG 混用。最新数据点实心圆高亮
- **饼图/环形图** — `conic-gradient` + 白色硬停止分割线。图例标注，最多 4 扇区
- **数据表格** — 数值列右对齐，表头底 + 品牌主色底线，峰值行高亮

### 品牌色板约束

仅 4 个品牌色可用于图表：`#D0121B`(primary) / `#AC000A`(primaryDark) / `#FE343F`(primaryLight) / `#FF777F`(accent)。禁止自造衍生色值（加亮/变暗/调饱和度）。色板不够靠线型（实线/虚线/点线）和标记形状（圆形/方形/菱形）区分。

### 字号阈值

常规（≤8 数据点）vs 密集（>8 数据点）两档阈值，覆盖 CSS 和 SVG 两种渲染方式。SVG 字号基于固定 viewBox，agent 无需计算 pt 映射。

### 验证记录

- `视觉参考/vi测试/chart-test.json` — 6月柱状图 + 3系列趋势 SVG + 12月折线图
- `视觉参考/vi测试/pie-donut-test.json` — 2×2 饼图+环形图网格
- `视觉参考/vi测试/dense-test.json` — 8列柱状图 + 10×10 表格（密集阈值边界验证）

---

## 2026-08-11 进展（下午）：方案 A 缩放修复 + Bug 修复

### 底部裁切修复
- generate.js 缩放公式从 `width/1920` 改为 `Math.min(width/1920, height/1080)`
- 取宽高缩放比的较小值，保证 1920×1080 画布始终完整可见
- 零副作用：renderer 不动、pt 字号体系不动

### Bug 修复
- **body 背景色**：`#111` → `#FFF`，与 slide 底色融合
- **点击翻页方向**：从 `show(current - 1)` 改为 `show(current + 1)`，点击前进到下一页
- **二进制雨可见性**：fontSize 18→28px，trail fade 3%→5%，canvas opacity 0.08→0.18

### 封面信息收集流程
- workflow 增加步骤：副标题/汇报人/部门/日期每项必须向用户确认，不得从未知来源填入

### 方案 B 待执行
- v2 视口原生方案（100vw×100vh + flexbox 替代固定画布 + scale）
- 涉及：generate.js CSS 壳 + cover/end/custom 三个 renderer px→vw/vh 迁移
- 不碰品牌数据层，与 PPTX 输出管线解耦
- 待方案 A 验证稳定后执行

---

## 2026-08-11 进展（上午）：合并恢复 + 内容页架构调整

### 合并丢失逻辑恢复
- 恢复 **Template/Themed 双模式**（cover.js 分派 `pages.scene`）
- 恢复 **hardRules** 块（Logo 安全区、深浅底、结尾页规则）
- 恢复 **Template/Themed 字号体系**（sizes.*.template/themed 双轨）
- 恢复 **前置步骤工作流**（判断模式→确认→透明声明）
- 恢复 **内页 Logo 尺寸**（113px → 80px，4.2% 画布宽）

### 内容页架构调整
- 新增 `custom` slide 类型：**agent 控制内容页排版**，skill 固定封面+结尾
- agent 约束：Logo 安全区、页面安全区（top:12%~bottom:25%）、品牌色值、字号
- Q3 报告验证：双卡片概览、+200% 红底大数字、四步流程图

### Bug 修复
- subtitle 渲染丢失 → 修复
- `NaN` JS 语法错误（删除 F 键残留 `+`） → 修复
- 底部 footer 被视口裁切 → 统一上移到 top:82-84%
- 自动全屏移除 → 仅 F11 手动全屏

### 待解决
- 视口原生方案：用 v2 的 100vw×100vh + flexbox 替代固定 1920×1080 + scale
- 跨工具 skill 一致性

---

## 2026-08-10 进展：bug 修复 + 品牌规则收紧 + ASCII 封面

### 修复

- **flex 压缩 bug**：`generate.js` — `#player` 缺 `flex-shrink:0`，视口 < 1920px 时 flex + JS scale 造成双重压缩。修复后播放器宽度正常。
- **正文色 bug**：`content.js` 正文用 `c.dark`（标题色），改为 `c.gray`。

### 品牌规则更新

| 规则 | 旧 | 新 |
|------|----|----|
| 背景系统 | 封面4选项 / 内页2选项 | 全部白色 |
| 正文颜色 | 无明确规则 | 标题=dark、正文=gray、强调=primary，禁止纯黑 |
| ASCII 字符画 | 无 | 存入 brand-tokens.json，禁止 AI 自生成 |

### ASCII 封面 Logo

- 263 心形 Logo 的 ASCII 字符画存入 `brand-tokens.json` → `coverAscii.art`
- 每行独立 `<span>` + JS `setTimeout` 逐行交错动画（奇数行左滑入、偶数行右滑入）
- 参数可调：`scaleX`(比例) + `overallScaleX/Y`(整体大小)，调至与真实 Logo 视觉一致
- 字号豁免：ASCII 装饰图形不受 16px 底线约束（`typography.asciiArt.exemption`）

### 文件清理

- 删除冗余 `skills/263-vi.md`，唯一真相源：`.claude/skills/263-vi/SKILL.md`
- 删除旧 VI skill 安装（`.claude/skills/263-vi/` 已移除，当前无安装）

### Code review 教训

- 渲染器 review 不能只读代码，必须浏览器实测：窗口缩放、翻页遍历、背景模式切换

---

## 2026-08-07 进展：领域建模 + VI Skill 重构计划

### 领域建模完成

通过 `/grill-with-docs` 会话，建立了两个限界上下文的领域模型：

- **品牌规范上下文** — 色彩、字体层级、Logo 规则、企业概况、产品组合（四层结构）
- **Agent 管线上下文** — Template/Themed 双模式、路径 A/B × Template/Themed 2×2 矩阵、skill 协作边界、输入格式分级

产出：`Design-Decision.md`（领域术语表），基于 263 集团业务介绍讲义（`视觉参考/`）提取的完整产品结构。

### 关键设计决策

| 决策 | 旧 | 新 |
|------|----|----|
| 字号底线 | 16px | 20pt（75寸电视@5m实测） |
| 生成模式 | 无区分 | Template（母版型）/ Themed（主题型），Agent 特征推理 |
| 用户交互 | 用户需了解 skill 才能用 | 透明声明："我将按XX模式制作，如用于YY请告诉我" |
| 产品结构 | 3板块×简易列表 | 3板块×4业务线×N产品能力（讲义稿真实数据） |
| 输入处理 | VI skill 自行解析 | 外部 Agent 调 MCP/工具 → 归一化后送入管线 |
| Logo 切换 | 仅集团/云通信二选一 | 品牌上下文：共享框架 + Logo 按业务线覆盖 |

### 实现计划

`docs/superpowers/plans/2026-08-07-vi-skill-align-context.md` — 6 个任务对齐 Design-Decision.md 设计树：

1. brand-tokens.json — 字体层级修正
2. brand-tokens.json — Logo+硬规则扩展
3. company-data.json — 产品四层结构重写
4. skills/263-vi.md — 核心工作流重写（双模式+2×2+透明声明）
5. Design-Decision.md — 权威数据源引用
6. 全量一致性验证

**状态：计划已写，待用户指令执行。**

---

## 设计框架

详细设计文档：`docs/superpowers/specs/2026-07-30-ppt-vi-template-system-design.md`

### 核心思路

**"品牌数据层，不是渲染引擎。"** — VI skill 是 263 品牌数据的唯一真相源。内容策划交给 LLM，排版渲染交给设计 skill。VI skill 只输出 brand-tokens.json + company-data.json。

### 两条路径 × 两种模式

```
用户需求
├── 路径 A（从零生成）
│   ├── Template → AGENT 按母版填空，全硬规则锁死
│   └── Themed  → AGENT + 设计 skill 从零创作
└── 路径 B（改写已有）
    ├── Template → 已有文件对齐母版规范
    └── Themed  → 已有文件 + 设计 skill 重设计
```

详细路径选择逻辑见 `Design-Decision.md` 和 `skills/263-vi.md`。

### 三层数据体系

| 层 | 内容 | Agent 可见 | 人可改 |
|----|------|:---:|:---:|
| 品牌视觉 (`brand-tokens.json`) | 色板、字体层级、背景选项、Logo 路径 | ✓ | 品牌部 |
| 公司数据 (`company-data.json`) | 简介、历程、产品、股票代码等 | ✓ | 品牌部 |
| 品牌指令 (`skills/263-vi.md`) | Skill 文件，教 agent 用品牌数据 | ✓ | — |
| 平台 skill 推荐 (`design-skill-recommendations.json`) | 平台 → 设计 skill 映射 | ✓ | 开发者 |
| 兜底渲染器 (`generate.js`) | HTML 渲染 + 播放壳（仅无设计 skill 时） | ✗ | 开发者 |

---

## 已完成

### 基础设施

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `.gitignore` | 排除视觉参考、.claude、node_modules、screenshots | ✓ |
| `brand-tokens.json` | 品牌视觉规范（替代 vi-tokens.json） | ✓ |
| `company-data.json` | 公司信息（合并 company-data/） | ✓ |
| `design-skill-recommendations.json` | 平台设计 skill 推荐 | ✓ |
| `skills/263-vi.md` | Claude Code VI skill 文件 | ✓ |

### 兜底渲染器（无设计 skill 时，从 pages.json 生成 HTML）

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `generate.js` | 渲染器入口：读取 pages.json → 输出自包含 slides.html | ✓ |
| `renderer/slides/cover.js` | 封面页渲染 — 自由背景、自由 Logo 布局 | ✓ |
| `renderer/slides/section.js` | 章节过渡页 — 大号编号 + 标题 + 分割线 | ✓ |
| `renderer/slides/content.js` | 通用内容页 — 标题 + 文本段落 | ✓ |
| `renderer/slides/cards.js` | 卡片网格 — N 列图标卡片 | ✓ |
| `renderer/slides/timeline.js` | 时间轴 — 年份节点 + 描述 | ✓ |
| `renderer/slides/end.js` | 结束页 — 居中 Logo + slogan PNG | ✓ |

**渲染器特性（兜底，仅无设计 skill 时使用）：**
- 定位：基础兜底模板，不做专业排版；专业排版交给设计 skill
- 播放壳：全屏/键盘翻页/翻页笔/点击翻页（PageDown/方向键/Space/点击）
- Logo 自动深底反白切换
- 背景系统：封面 4 选项，内页限定白/浅灰
- 字号硬约束 < 16px 拒绝
- 零外部依赖，输出单一 HTML 文件
- 不提供：设计装饰、复杂布局变体、悬停动效（由设计 skill 负责）

### VI 应用器（路径 B）— 已移除

路径 B 功能由设计 skill 替代。`vi-apply.js` 已删除；已有 HTML PPT 的视觉改造交给专业设计 skill，其读取 `brand-tokens.json` 统一色值/字体/Logo，布局由设计 skill 决定。

### 公司数据

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `company-data/facts.json` | 公司名、股票代码、办公地址、品牌理念、23 条发展历程 | ✓ 官网核对完成 |
| `company-data/products.json` | 三大业务线：全球网络 / 智能通信 / 数字服务 | ✓ |
| `company-data/profile-zh.md` | 公司简介长文本 | ✓ |
| `company-data/profile-en.md` | 英文简介 | ✓ 基础版 |

### 资产

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `assets/logos/logo-group-color.png` | 集团红心 Logo | ✓ |
| `assets/logos/logo-group-white.png` | 集团反白 Logo | ✓ |
| `assets/logos/logo-cloud-color.png` | 云通信蓝 Logo | ✓ |
| `assets/slogan.png` | 「连接世界 沟通你我」固定字体设计 | ✓ |

### 示例

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `examples/sample-pages.json` | 8 页示例 PPT（公司简介场景） | ✓ |
| `examples/ai-tools-pages.json` | 13 页示例 PPT（AI 工具心得，真实内容测试） | ✓ |

### 文档

| 文件 | 用途 | 状态 |
|------|------|:--:|
| `docs/superpowers/specs/2026-07-30-ppt-vi-template-system-design.md` | Phase 1 设计文档 | ✓ |
| `docs/superpowers/plans/2026-07-30-ppt-vi-template-system-plan.md` | Phase 1 实现计划 | ✓ |
| `docs/superpowers/specs/2026-07-31-vi-skill-redesign.md` | VI skill 重构设计文档 | ✓ |
| `docs/superpowers/plans/2026-07-31-vi-skill-refactor.md` | VI skill 重构实现计划 | ✓ |

---

## 已验证

### 路径 B：已有 HTML 改写 VI ✓

已用 `8-AI工具心得-杜鸣-V8.html`（14 页，真实工作内容）验证完整链路：
1. VI skill 确认品牌参数 → 输出 brand-tokens.json
2. frontend-design skill 指导排版（响应式、字体层级、卡片设计）
3. Logo base64 嵌入、安全区无遮挡、结尾页 VI 规范格式
4. 色值精确替换（#D0111B → #D0121B）、CSS 变量统一管理

**结论：路径 B 已验证通过。** 已有 HTML PPT 可通过 VI skill + frontend-design 快速统一品牌视觉。

### 路径 A：从零生成 → 待测试

需用真实需求（如"做一份云通信产品介绍PPT"）测试完整流程：LLM 策划内容 → pages.json → 设计 skill 排版。

---

## 待继续 / 未完成

- [ ] **执行 VI skill 重构计划** — `docs/superpowers/plans/2026-08-07-vi-skill-align-context.md`，6 个任务
- [ ] **路径 A 从零生成测试** — 用真实业务场景（如"云通信产品介绍"）端到端测试
- [x] **Claude Code 设计 skill 调研** — 确认 `frontend-design` 优于 `ui-ux-pro-max`，已填入 design-skill-recommendations.json
- [x] **Logo 安全区规则** — 已写入 skills/263-vi.md：Logo 周围禁止装饰元素遮挡
- [x] **产品结构细化** — 从集团介绍讲义提取，3板块×4业务线×N产品能力，写入 Design-Decision.md
- [x] **领域建模** — Design-Decision.md 已创建，两个限界上下文 + 完整设计树

### 资产和配置待补充

- [ ] **商务蓝衍生色值** — 亮蓝/暗蓝/浅蓝背景目前是推算值，需官方确认
- [ ] **云通信 Logo 反白稿** — 文件 `视觉参考/logo-云通信-白.png` 已存在，待放入 `assets/logos/`

### 功能扩展

- [ ] **错误处理增强** — 非法 JSON 结构时给出明确的错误信息（而非 Node.js 堆栈）

### 待调研

- [ ] **Web chatbot 适配** — 当前系统依赖 Node.js 命令行，纯 web chatbot（ChatGPT 等）无法调用。需评估：
  - 方案：将 `generate.js` 逻辑搬到前端页面，chatbot 输出 pages.json → 粘贴到页面 → 浏览器端渲染 → 预览/下载
  - 工作量：generate.js 核心逻辑改写为前端 JS（处理 base64 图片嵌入） + 一个静态页面
  - 是否需要？什么时候做？待决定

### Phase 2（待规划）

- [ ] 可视化 pages.json 编辑器
- [ ] 内页布局变体（左文右图 / 上文下图 / 分栏 / 大数字 + 描述等）

### Phase 3（待规划）

- [ ] .pptx 渲染器

---

## 当前项目结构

```
263viForAgent/
├── Design-Decision.md                ← 领域模型（两个限界上下文）
├── generate.js              ← 兜底渲染器入口（仅无设计 skill 时）
├── brand-tokens.json        ← 品牌视觉规范（色板/字体/Logo/背景）
├── company-data.json        ← 公司信息（合并 facts + products）
├── design-skill-recommendations.json ← 平台设计 skill 推荐
├── skills/
│   └── 263-vi.md            ← VI skill 文件（品牌数据层入口）
├── company-data/
│   ├── facts.json
│   ├── products.json
│   ├── profile-zh.md
│   └── profile-en.md
├── assets/
│   ├── logos/
│   │   ├── logo-group-color.png
│   │   ├── logo-group-white.png
│   │   └── logo-cloud-color.png
│   └── slogan.png
├── renderer/
│   └── slides/
│       ├── cover.js
│       ├── section.js
│       ├── content.js
│       ├── cards.js
│       ├── timeline.js
│       └── end.js
├── examples/
│   ├── sample-pages.json / .html
│   └── ai-tools-pages.json / .html
├── docs/
│   └── superpowers/
│       ├── specs/ (设计文档)
│       └── plans/ (实现计划)
└── 视觉参考/ (不入 git)
```

## Git 历史

```
a6ec19d fix: recommend frontend-design over ui-ux-pro-max for Claude Code
5261f89 docs: reconcile PROJECT-STATUS — remove stale references to deleted files
9f0e56e fix: guard missing white logo variant in generate.js
534f399 docs: update project status to reflect VI skill refactoring
bfeee91 refactor: remove vi-tokens.json — replaced by brand-tokens.json
ee49245 refactor: remove deprecated files — replaced by brand data layer + design skill delegation
025b69c fix: read brand-tokens.json, hardcode slide dimensions in renderers
04a4ff5 feat: add 263-vi skill (Claude Code VI spec entry point)
44b70e9 feat: add design-skill-recommendations.json platform skill mapping config
9f15685 feat: merge company facts and products into single company-data.json
b973662 feat: add brand-tokens.json (brand visual data, slide dims removed)
--- Phase 1 ---
7904e06 feat: redesign end slide — centered logo + slogan, no text
b066118 feat: add slogan image to end slide
481c1eb chore: remove .claude/ from git tracking
0e025f1 chore: remove strategy field from company data
89084b7 feat: update products to three business lines, fix vision, remove values
e3518d9 feat: update company data from official website
4e206a8 feat: add background system — cover free choice, inner pages white/light-gray only
94351b0 fix: switch logo rendering from img+content to div+background-image
30f04b5 chore: remove temporary preview files
bf74fea feat: add agent-prompt.md and sample pages.json with verified output
a0d82d9 feat: add renderer core, player shell, and 6 slide type renderers
5349553 feat: add logo assets for group and cloud variants
e10bc3e feat: add schema.json and company data layer
2b0db08 feat: add vi-tokens.json with dual color schemes and typography
0bef567 plan: add Phase 1 implementation plan with 14 tasks
... (design docs)
```

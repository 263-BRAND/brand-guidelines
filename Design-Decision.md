# 263 VI for Agent — Domain Context

两个限界上下文：**品牌规范**（稳定的品牌数据）与 **Agent 管线**（路径路由 + skill 协作）。

---

## 一、品牌规范

### 色彩体系 (Color Scheme)

| 色彩方案 | 主色 | 状态 |
|---|---|---|
| 集团红 | `#D0121B` | 已启用 |
| 通信蓝 | 待官方确认 | 暂不可选（规划中） |

通信蓝与集团红共享同一套品牌规范框架，仅色板不同。

### 品牌上下文 (Brand Context)

一次生成任务中生效的品牌参数组合。包含：

- **共享继承：** 色板、字体层级、硬规则（从集团规范继承）
- **可变覆盖：** Logo — 按业务线切换（如云通信 Logo 替换集团 Logo）

业务线使用自己的产品 Logo 时，其他品牌规则不变。

### 正文颜色 (Text Colors)

三层体系，严格对应：

| 层级 | 色值 | 用途 |
|------|------|------|
| 标题色 | `dark` (#2D3847) | 页面标题、卡片标题、章节标题 |
| 正文色 | `gray` (#595959) | 段落文本、描述、辅助信息 |
| 强调色 | `primary` | 链接、标签、重点标记、分割线 |

禁止使用纯黑 `#000000` 作为文本色。

### 字体层级 (Typography Scale)

以 75 寸电视 @ 5m 会议场景为基准，兼顾笔记本屏幕。正文字体：微软雅黑。

| 层级 | HTML (pt) | PPTX (pt) | 刚性 |
|---|---|---|---|
| 封面标题 | 64 | 43 | Template MUST / Themed 区间 |
| 内容页标题 | 40 | 24 | Template MUST / Themed 区间 |
| 副标题 | 30 | 20 | Template MUST / Themed 区间 |
| 正文 | 26 | 18 | MUST |
| 图表标签/注脚 | 22 | 14 | MUST |

**跨格式直出：** HTML 加 `pt` 后缀，PPTX 写数字（原生单位即 pt），禁止 pt↔px 换算。

> PPTX 封面标题 43pt（非 64pt）：CSS pt 在 1920×1080 和 PowerPoint pt 在 960×540 解析不同——同数值视觉占比偏差大（HTML 7.9% vs PPTX 11.9%）。43pt 对齐 HTML 64pt 比例。

ASCII 字符画（封面 Logo 图）属于装饰图形，不受字号底线约束。

### 字体回退链

`微软雅黑, Microsoft YaHei, Noto Sans SC, Source Han Sans SC, sans-serif`。Noto Sans SC（思源黑体，SIL OFL）为跨平台开源回退，Mac/Linux/纯 Web 环境下替代微软雅黑。

#### 行距规范决策记录（2026-08-17）

**背景：** Trae 实测发现内容页行距偏小——渲染器硬编码 `line-height:1.6`，且 Template 硬规则三项（字号/颜色/Logo 位置）不含行距，1.6 是渲染器作者随手值。

**决策：**
- 行距改为**品牌定值**，写入 `brand-tokens.json` → `typography.lineHeight`（HTML/PPTX 两套，禁止互套）
- **HTML**（CSS line-height 乘数）：主标题 1.3、小标题/正文/卡片/时间轴 1.8。标题 1.3 非 2.0——2.0 让单行标题盒高 2×字号、文字垂直居中后下探撞进固定 top:28% 内容区，造成标题/小标题重叠
- **PPTX**（倍距，用户习惯）：标题单倍(100%)、其余 1.2倍(120%)。PPTX 与 HTML 测量单位不同：PPTX 1.2倍 ≈ HTML ~1.6，不可直接复刻数值
- 渲染器从 token 读取行距，消除硬编码魔法数字
- 三页标题位置统一 `top:12%`（content 原 18% 与 cards/timeline 12% 不一致）

#### 行距覆盖扩展 + 目录页 toc 类型（2026-08-17 追加）

**背景：** Trae 实测行距规范漏掉章节页（section）与目录页。且系统无 toc 渲染器，目录通常用 content 拼凑。

**决策：**
- 行距表补齐：章节页标题 1.3/单倍、章节页副标题 1.8/1.2倍、目录页标题 1.3/单倍、目录条目 `tocItem` 1.8/1.2倍
- 章节页大号数字底纹行距独立 `sectionNumber`：HTML 2.0 / PPTX 1.2倍——大号数字 64pt，1.3 行距盒高过小、数字与标题贴近；2.0 拉开垂直空间（行盒 128px），视觉透气。字号仍复用封面标题 64pt（非独立 token，用户未要求解耦）
- **新增 toc 渲染器**（`renderer/slides/toc.js`）：slide 字段 `title` + `items[]`（每项 `index` + `text`），对齐 content 版式骨架（白底/右上 Logo/top:12% 标题/28% 内容区/底部页码）。`generate.js` slideTypes 加 `toc` → 共八种 slide 类型
- 浏览器实测通过：toc 目录项、章节页带/不带副标题、缩放适配、翻页遍历

### 画布尺寸

| 格式 | 尺寸 | 单位 |
|------|------|------|
| HTML | 1920×1080 | px |
| PPTX | 960×540 | pt（PowerPoint 16:9 宽屏默认） |

所有百分比规则按各自画布基准换算。PPTX 尺寸值从 `brand-tokens.json` → `layout.pptx` 读取。

### 背景系统

所有页面统一白色底色，不提供背景选项。封面和内页均为 `#FFFFFF`。

### ASCII 字符画（封面 Logo）

263 心形 Logo 的 ASCII 字符画存入 `brand-tokens.json` → `coverAscii.art`。严禁 AI 自行生成或修改，必须从 token 原样读取。渲染规则：

- `<pre>` + `white-space:pre` + `font-family:monospace` 确保跨平台对齐
- 每行独立 `<span class="ascii-line">`，奇数行左滑入、偶数行右滑入
- JS `setTimeout(i × 30ms)` 逐行交错触发，0.6s 过渡，全部完成后标题淡入
- 参数：`font-size:11pt`、`line-height:0.6`、`scaleX:1.8`、`overallScaleX:0.576`、`overallScaleY:0.528`

### Logo 规则

| 规则 | 刚性 |
|---|---|
| 内页 Logo 尺寸：画布宽度 4.2%（`layout.innerPageLogo.width/height` 正方形） | MUST |
| 封面 Logo（Themed）：画布宽度 6.5%（`layout.coverLogo`），左上角 | MUST |
| 封面 Logo（Template）：不使用 PNG，以 ASCII 字符画替代 | MUST |
| Logo 安全区内禁止任何装饰元素、文字、页码 | MUST |
| 浅色底使用彩稿 Logo | MUST |
| 深色底使用反白 Logo | MUST |
| 结尾页：居中 Logo + slogan PNG（不可用文字代替） | MUST |
| 图片等比缩放：HTML `background-size:contain` / PPTX `LockAspectRatio=true` | MUST |
| Logo 容器必须正方形（宽=高），Slogan 只固定宽度 | MUST |
| PPTX 图片嵌入：内嵌二进制，禁外部链接，LockAspectRatio 顺序：插入→锁比例→设单维度 | MUST |
| PPTX 封面文字框必须透明背景（`FillVisible=false`） | MUST |

### 硬规则 (Hard Rules)

Template 路径全部锁定。Themed 路径在硬规则约束内由设计 skill 自由发挥。

### 企业概况 (CorporateProfile)

独立聚合，与产品组合生命周期分离。包含：公司简介、成立年份（1997）、上市信息（002467.SZ）、总部（北京）、全球 12 处办公地点、15 万企业客户、700 万企业用户、320 万个人及家庭用户、资质荣誉、594 项软件著作权、23 项专利。

### 产品组合 (ProductPortfolio)

独立聚合。三大板块，每板块四条业务线，每业务线下含具体产品/能力。

#### 板块一：全球网络（筑牢通信基石）

| 业务线 | 产品/能力 |
|---|---|
| **环球专线** | IPLC/IEPL 专线；海缆 APG/PEACE/JUPITER；优质传输、计费灵活、端到端一站式交付 |
| **国际专网** | SR-MPLS（底层传输）；SD-WAN（中层组网，100+城市/45+国内节点）；SASE（上层安全：零信任+云防火墙） |
| **数据中心** | IDC托管，SLA 99.9999%（六个9）高可用；运营商中立；Tier 3+；全链路冗余；与NTT合资上海高端机房；异地双活；7×24安保；DCI互联：北京/上海/杭州/香港/东京/新加坡 |
| **移动通信** | 一卡双号（跨境特色）；语音处理平台；AI内容风控（AI+人工双重质检）；套餐定制 |

#### 板块二：智能服务（赋能千行百业数字化升级）

| 业务线 | 产品/能力 |
|---|---|
| **呼叫中心 AICC** | 智能质检（全量转译+情绪识别）；智能客服（7×24 AI，80%自助解决）；智能外呼（AI语音交互）；虚拟坐席（实时话术推荐） |
| **企业直播** | 自研RTC毫秒级低延迟；3D虚拟/数字人直播；全流程管家式服务（年约6000场）；覆盖营销/培训/私域/3D虚拟场景 |
| **企业邮箱** | 263云邮（标准SaaS）；信创SaaS（首款信创公有云邮箱）；私有云邮箱；AI：智能过滤/分类/摘要 |
| **智能终端** | 企业：VCX-6/6M/9Z/3 会议终端；家庭：家庭电话/安防监控/数字电视盒子 |

#### 板块三：数字内容（数据驱动+智能体技术）

| 业务线 | 产品/能力 |
|---|---|
| **数字员工** | 智能问答与办公助理；会议与文档智能化；企业知识沉淀；数字分身；数字IP：品牌代言/直播带货/客服接待/培训讲解 |
| **智能体平台** | 低代码创建与部署；垂直场景适配；多模型接入与管理；TokenHub企业级算力调度 |
| **知识管理** | 结构化/半结构化/非结构化知识；与智能体直接关联；智能创建知识框架 |
| **内容质检** | 全量实时监测；AI+人工双重保障；灵活规则配置（关键词/情绪/意图/语气）；全链路内容风控 |

---

## 二、Agent 管线

### 生成模式 (Generation Mode)

内部术语，对用户不可见。Agent 从用户自然语言中判断。

| 模式 | 特征 |
|---|---|
| **Template（母版型）** | 受众在组织内部；目的是汇报/同步/述职；设计上要求统一规范而非个性表达 |
| **Themed（主题型）** | 受众在组织外部；目的是说服/展示/介绍；设计上允许在品牌框架内发挥个性 |

Agent 按特征推理，不硬匹配关键词。用户自然语言纠正即可切换模式。

### 前置步骤（四条路径共用）

1. **判断模式** → Template 还是 Themed（硬锚词优先）
2. **硬门禁确认** → 按场景分支逐一询问（工作汇报 → 场景/配色/输出格式/封面风格；对外展示 → 场景/配色/输出格式/视觉风格——**先问输出格式、再问视觉风格**）。**Agent 先判断场景，但判断后也必须询问用户确认，禁止跳过**。对用户说的话只能逐字来自 SKILL.md「生成前确认 → 用户话术」（唯一输出源），禁止暴露内部文件名。**交互措辞三铁律**：① 确认问题逐字原样用固定话术；② 禁止向用户播报判断依据（命中/锚词/锁定等推理词不出现）；③ 内部词汇与文件名的「用户话术替换对照」见 SKILL.md「生成前确认 → 内部思考词汇」。**封面风格推荐与输出格式联动（双向强制）**：选 PPT 文件 → 必须推荐严谨商务风格；选网页文件 → 禁止推荐严谨风格。**视觉风格推荐（对外展示第 4 题）**：一律推荐「搭配额外专业美化技能」，搜索按第 3 题输出格式定向。**PPT 输出路径（第 3 题选 PPT 后，2026-08-18 追加；判断标准 2026-08-18 收紧）**：原生 PPTX 排版能力只看一条客观事实——对话中已加载**能直接产出 .pptx 版式**的专业PPT制作技能或设计类技能、或调用过设计类 sub-agent（**263group-brand-guidelines 本身不算**——品牌数据层，只产品牌数据与 HTML，产不出 .pptx；你的代码能力也不算——任何工具都能写代码）。已加载 → 直接交给该技能/agent；未加载 → 走话术 3a 推荐网页文件或用专业PPT制作技能或设计类技能；用户坚持代码生成 → 生成前警告（话术 3b）+ 读取 `pptx-python-guide.md` 实现要点
3. **确定品牌上下文** → 配色 + Logo 归属
4. **读取品牌数据** → brand-tokens.json（必须）

### 输出透明声明

生成结果开头告知用户当前模式，降低误判纠正门槛。格式：

> "我将按对外展示（或：工作汇报）的设计方式制作。如果你用于[另一种场景]，请告诉我。"

### 路径 × 模式（2×2 矩阵）

| | 路径 A（从零生成） | 路径 B（改写已有） |
|---|---|---|
| **Template** | Agent 按母版填空，生成新文件 | 已有文件对齐母版规范 |
| **Themed** | Agent + 设计 skill 从零创作 | 已有文件在品牌约束内重新设计 |

### Skill 协作边界

| 层 | 负责方 | 内容 |
|---|---|---|
| 品牌数据层 | 263group-brand-guidelines | 色彩、字体、Logo规则、结尾页格式 |
| 表现层 | 设计 skill | 排版布局、装饰元素、图表风格、视觉层次 |

Template 路径：263group-brand-guidelines 全控，设计 skill 不介入。
Themed 路径：263group-brand-guidelines 提供品牌数据 + 硬规则约束，设计 skill 在约束内自由发挥。

### 输入格式分级

263group-brand-guidelines 不负责解析文件。外部 Agent 自行调用 MCP 或工具摘取内容后送入管线。

| 分级 | 格式 | 可靠性 |
|---|---|---|
| 原生支持 | HTML、JSON | 路径 B 完整能力 |
| 尽力而为 | .pptx、.docx、.pdf、图片 | Agent 尽力提取 + VI 化，不保证完美 |

### 数据图表规范

图表生成受品牌色板 + 字号阈值 + 安全区三重约束。详见 SKILL.md → 数据图表规范。

- **色板（chartPalette）：** 7 档红系色阶是**唯一品牌红尺度**（浅 `#F8B5B8` → 深 `#72090E`；`colorSchemes.primary/primaryLight/primaryDark` 均为其档位指针）+ 非重点中灰 `#BFBFBF` + 远距红组（s1/s4/s7，两两 ΔE ≥ 25）。浅粉 `#FF777F` 已从图表移除，降为纯装饰。存储于 `brand-tokens.json` → `chartPalette`。通信蓝色板待官方确认后按同法定义
- **上色规则（优先级）：** ① 重点突出——1~2 个重点数据用品牌红 `#D0121B`，其余用中灰 `#BFBFBF`（agent 从用户需求自动判断重点）；② 色阶方向——无重点时按用户自定义顺序铺色阶，第一行/列取一端（最浅或最深），方向必须二选一全图一致。① > ②，禁止混用
- **二次编码：** 色阶相邻档 ΔE 低，靠线型+标记（折线）、数值标签（柱状）、白分割线+扇区标签（饼/环）承载系列身份
- **安全区：** 图表所有组成部分（图形、坐标轴、刻度、标注、图例）完整约束在 top:12% ~ bottom:18%
- **字号阈值：** 常规 ≤8 数据点 / 密集 >8 数据点，CSS 和 SVG 各自有阈值表
- **折线图：** 单 SVG + 固定 `viewBox="0 0 740 520"`，禁止 CSS/SVG 混用
- **饼图/环形图：** `conic-gradient` + 白色硬停止分割线，扇区数 ≤ 7

#### 图表配色决策记录（2026-08-17）

**背景：** Trae Work 测试 PPTX 流程时，agent 无法执行图表颜色要求，认为原 4 色红系（`#D0121B`/`#AC000A`/`#FE343F`/`#FF777F`）视觉区分度太低，自行造色。原规则"仅 4 品牌色 + 靠线型/图案区分"对 PPTX 原生图表不成立（原生系列不支持图案填充，饼图无线可画线型、无点可放标记）。

**验证：** 用 dataviz 验证器实测：4 红系相邻 ΔE 仅 10.9（`#FF777F`↔`#FE343F`），Trae 的 7 档色阶顺序填充最差对 ΔE 6.5（`#D0121B`↔`#EC2831`），均不达标。**单色相色系色彩单通道最多承载 2~3 个高区分身份**——这是物理限制，非品牌缺陷。

> **2026-08-18 勘误：** 原记录标注"OKLab ΔE"为标签错误。OKLab 欧氏距离对这两对色仅 0.1 量级，记录中 10.9/6.5 的量级对应 **CIELAB ΔE2000**（复现：`#FF777F`↔`#FE343F` ΔE2000=12.8、`#D0121B`↔`#EC2831`=7.5，与记录值基本吻合）。后续色板验证一律用 ΔE2000。

**决策：**
- 采纳 Trae 7 档红系色阶为官方图表色板（人类肉眼验证可接受，两端浅粉白/近黑褐保留）。色阶作**渐变方向**而非独立身份
- 引入**重点突出规则**绕开"每系列独立色"困境：1~2 个重点用品牌红，其余统一浅粉/淡灰，重点与非重点 ΔE ≥ 20
- 引入**色阶方向规则**：无重点时按用户自定义顺序铺色阶（第一行/列取一端），顺序本身承载身份，颜色表方向不表大小
- 排除：玫红 `#D63384`、绛紫 `#A6326E`（偏离主色色系，品牌决策）；通信蓝不入集团红色板（暂不可选）
- 新增淡灰 `#BFBFBF` 作为非重点静默色（科技灰 `#716F69` 偏深，浅粉 `#FF777F` 可作暖调替代）

#### 品牌色板统一决策记录（2026-08-18）

**背景：** 评估发现两套互不统一的红色系（`colorSchemes` 的 primaryLight/primaryDark 与 `chartPalette` 7 档），旧三红内部已有低区分对（primaryLight `#FE343F` vs primary ΔE2000=12.4、primaryDark `#AC000A` vs primary=7.8，均 <15）；`#FF777F` 同时是 accent、图表非重点、且与色阶 s2 ΔE 仅 3.7，三身份冲突。

**ΔE2000 实测（2026-08-18）：**
- 7 档色阶全部 6 个相邻对 7.5~11.5，均 <15 → 色阶只表方向不表身份，维持既有定位
- 浅粉 `#FF777F` vs s2 `#F4868B` = 3.7（几乎同色）、vs s3 = 7.8 → 与色阶浅半区结构性无法共存
- 中灰 `#BFBFBF` vs 全 7 档 ≥20.2 → 唯一安全非重点
- 远距红组 s1 `#F8B5B8`/s4 `#EC2831`/s7 `#72090E` 两两 ≥25 → 需 2~3 个可区分红色时取此组
- 语义深绿 `#3E8E4E` vs 品牌红 ΔE=65.0、白字 4.05:1 → 涨跌语义色安全

**决策：**
- **统一红色尺度**：7 档色阶为唯一品牌红系统。primary = s5 `#D0121B`（不变）；primaryLight 由 `#FE343F` 改为 **s3 `#F0575E`**（vs primary ΔE 16.1，修复 12.4 低区分）；primaryDark 由 `#AC000A` 改为 **s7 `#72090E`**（vs primary ΔE 18.8，修复 7.8 低区分，Themed 封面渐变深端变深）
- **小标题用品牌红**：content 页内容小标题（h3）由 primaryLight 改为 primary `#D0121B`，对比度 3.37 → 5.55:1
- **卡片底用结构浅灰**：cards.js 卡片底由白改为 lightGray `#F2F2F2`（深字 10.61:1，白页上柔和浮起面）。`#BFBFBF` 不充当卡片底（vs 白页边界 1.84 太重，且为图表非重点专用）
- **浅粉降级**：`#FF777F` 从 chartPalette muted 移除，定死为装饰色（封面点缀/强调线条），禁止进图表数据
- **新增语义色**：`semantic`（positive=品牌红 / negative=`#3E8E4E` 深绿 / neutral=`#BFBFBF` 中灰），中国惯例**红涨绿跌**——品牌红天然承担"涨/正"，绿承担"跌/负"，避开用品牌红表坏消息的冲突。警示橙不采纳。**使用域封死（2026-08-18 测试后追加）：深绿仅限图表涨跌数据，禁止用于非图表设计**（Trae 实测 agent 在 A/B 对比误用红+绿）——非图表两项对比用品牌红 vs 中灰/深蓝灰
- **命名澄清**：`#F2F2F2` = 结构浅灰（卡片底/表头底/斑马纹/分割线），`#BFBFBF` = 图表中灰（仅图表非重点），两灰唯一命名，消除"淡灰"歧义

#### Themed 对外展示路径开放决策记录（2026-08-18）

**背景：** 用户提出对外展示版本开放设计自由度（遵循品牌规范 + 公司数据），封面不固定、结尾页固定（同 template）、字体开源。现状核对：结尾页已固定、Themed 表现层已自由——真正要改的是封面开放 + 兜底封面图 + 字体分流。

**决策：**
- **封面开放 + 品牌底线**：Themed 封面由设计 skill 自由设计（版式/色板内配色/装饰/动效），但品牌底线锁死——色板 token 禁自造、封面必须带 263 品牌标识且 Logo 安全区零容忍、公司数据不编造、禁纯黑、字号底线、字体开源、封面信息来自 pages.json 字段
- **兜底封面图**：`assets/cover-themed-fallback.png`（源图 `视觉参考/background-red-themed_v1.png`，用户提供，**暂用稿，后期可能替换**）。**渲染图无关**（路径从 token 读，换图只动 PNG、不动代码与规则）。**触发（2026-08-19 纠正为默认）**：渲染器渲染 Themed 封面时默认即兜底封面图（无需背景键）；`"background": "themed-fallback"` 保留作显式别名；设计能力判定同 PPTX 客观尺子（263group-brand-guidelines/代码能力不算，仅加载 263group-brand-guidelines → 兜底图）。模式同 red-template：位图底 + 文字叠加，HTML/PPTX 100% 一致。入库统一白底压平（原图 RGBA 带透明，白底保证 PPTX 背景确定）
- **封面 Logo 禁止反白**（用户补充）：封面 Logo 一律彩稿 `logo-group-color`，不随深浅底切换；深色封面彩稿不可读时调布局（Logo 置浅色区）而非换反白
- **字体分流**：工作汇报（内部）微软雅黑栈；对外展示（外部）开源栈 `Noto Sans SC, Source Han Sans SC, sans-serif`（token 新增 `typography.fontFamilyOpenSource`）——微软雅黑闭源，对外分发/嵌入有许可风险（SIL OFL 开源无风险）

**spec：** `docs/superpowers/specs/2026-08-18-themed-open-cover-design.md`（2026-08-18 提交，设计已确认）
**状态：** 已实现（2026-08-19）——兜底封面图入库 + 渲染器支持 themed-fallback + 字体按场景分流 + zip 19→20，浏览器实测通过

#### 封面背景禁红色系决策记录（2026-08-19）

**背景：** 跨工具测试发现 Themed 封面 Logo 规则有漏洞——agent 用色阶红色或渐变红作封面背景（`primary-gradient`/`primary-solid`），彩稿 Logo 主色即品牌红 `#D0121B`，红底同色被吞。现有规则「深色封面 → Logo 置浅色区」是软约束，且红底既不算深色也不算浅色，agent 容易不触发。用户主张**不反白是底线**——三方案对比（浅色衬底 / 背景禁红 / Logo 区浅色块）后选择**背景禁红**（最直接、零漏洞）。

**决策：**
- **封面背景禁红色系**：品牌红 `#D0121B`、色阶 s1-s7、primaryLight `#F0575E`/primaryDark `#72090E`、accent `#FF777F` 及其任何渐变/组合——封面一律禁止。彩稿 Logo 主色即品牌红，红底会吞 Logo
- **同时禁 `dark-solid`**：深底触发反白 Logo（`logo-white-img`），违反「封面 Logo 一律彩稿禁反白」底线——一并剔除
- **合法封面背景收敛为两个**：兜底封面图（默认，themed-fallback）或 `white`；设计 skill 可在色板内拼非红浅色组合
- **落点（四处）**：brand-tokens.json `backgrounds.cover` note 标注；SKILL.md 品牌底线新增第 3 条 + 封面背景选项剔除红系；generate.js 封面背景校验 fail-loud（`primary-gradient`/`primary-solid`/`dark-solid` 封面即报错 exit 1）；cover.js `renderThemed` 简化为浅底（white）强制彩稿 Logo，删除 isDark 反白分支
- **不受影响**：red-template 严谨封面（固定红色位图底但 Logo 在左半白色文字区，不吞 Logo）；Template ASCII 封面（白底）

**验证：** node 实测——white/默认兜底图正常生成，primary-gradient/dark-solid 被校验拦截 exit=1；headless Chrome 截图像素分析——white 封面纯白底 + 深色标题 `#2D3847` + 彩稿 Logo（对比度 3.39:1），兜底图封面浅蓝灰底 + 红心 Logo 轮廓清晰
**状态：** 已实现（2026-08-19），zip 重建

#### 封面排版决策记录（2026-08-17）

**背景：** 严谨风格（red-template）封面在 Trae PPTX 测试中需要规范化排版。

**决策（仅适用于 red-template 封面，ASCII 封面跳过）：**
- **左上角集团红心 Logo**：复用集团彩稿 `logos.group.color`（与内页一致，非新增文件），位置固定沿用 Themed 封面参数 `left:6%/top:8%/125px` 正方形（PPTX 62pt，坐标 57.6/43.2pt）。此前 red-template 封面不放置 PNG Logo，本次改为必放
- **标题字数**：≤20 字符/行、禁止折行；超限 Agent 建议改短或自行分两行（`\n`）；宽度 `white-space:nowrap` 自适应、上限 50%（20 字符 @64pt ≈ 44%，白区安全）
- **副标题独立行**：30pt 非粗，不与汇报人/部门/日期同行（原合并一行，本次拆分）
- **汇报人/部门/日期**：26pt（小于副标题），圆点分隔
- **文字块垂直居中**：`startY = (SH − totalH) / 2`。验证：白区实际延伸至 y≈920（85% 高度），最坏情况（2 行标题+副标题+meta≈350px）居中后底部 ~715px 仍安全在白区
- **间距分层防重叠**：标题→副标题 16px，副标题→汇报信息 32px；任何文字元素禁止重叠
- 公司全称 `bottom:6%` 不动（文字信息块不含底部）

---

#### ASCII（个性化）封面排版决策记录（2026-08-19）

**背景：** 用户实测工作汇报·个性化（ASCII）封面 PPTX，外部代理读 SKILL 百分比位置（`top:48%/62%/82%`）常算错，主标题/副标题错位约 19pt。要求：位置/字号**写死**、主标题**不可折行**、**水平居中对齐背景图**。目标位置由用户验收截图校准（`测试记录/ScreenShot_2026-08-19_133152_977.png`，PIL 实测视觉中心 55.4%/64.5%/84.5%）。

**决策：**
- **PPTX 文字坐标一律写死 pt 绝对值**（全宽框 `left=0/width=960` + 水平居中 + 垂直居中，文字中心恒为画布 50%，天然对齐背景图 ASCII 中心；禁止百分比换算）：
  - 主标题 `top=Pt(277)`/`h=44` 43pt Bold dark，**不可折行**（视觉中心 55.4%）
  - 副标题（如有）独立一行 `top=Pt(337)`/`h=22` 20pt gray（64.5%）
  - 汇报人·部门·时间独立一行 `top=Pt(359)`/`h=22` 18pt gray，品牌红圆点·分隔（68.5%）
  - 公司全称 `top=Pt(449)`/`h=14` 14pt gray（84.5%）
  - 副标题为空 → 汇报人落 `Pt(337)`；行距 22pt 行单位；汇报人行底 ≈381pt 与公司名顶 449pt 不重叠
- **副标题独立一行（HTML/PPTX 同规则）**：HTML 渲染器 `renderTemplate` 拆分合并行为「副标题 30pt + 汇报人·部门·日期 26pt」两行（镜像 red-template/themed），补 `date` 字段
- **主标题禁止 `\n` 手动分行**：固定 44pt 单行框，两行会与副标题重叠；red-template 封面 `\n` 规则不受影响（flex 自适应）
- **字号对齐字体表 PPTX 列**：副标题 20pt > 文字信息 18pt；`coverMetaLine()` helper 统一三封面 meta 行（DRY，refactor 字节级零变化）

---

#### 内页 Logo 安全区 X/Y 双轴决策记录（2026-08-18）

**背景：** slidep 实测发现内页右上角 Logo 盖住右侧红色卡片/图形。agent 计算正确，根因是 spec 数值自相矛盾：
- 内容区 `top:12%`（129.6px）在 Logo 底边（`top:5.3%` + `width:80px` = 137.2px）之上 → Y 已相交
- SKILL 原「内容区最大右边界 91.6%」按 `right:4.2%` 旧值算，token 实际 `right:5.2%` → 真实边界 90.6%；91.6% 本身侵入 Logo 框 19px
- 渲染器 `right:6%`（94%）侵入 65px
- 连带：SKILL 多处引用不存在的 `layout.innerPageLogo.size` 键（token 实际键名 `width`/`height`）——契约键名漂移

**决策：**
- **安全区 = Logo 矩形边界框，X/Y 双轴都算**：X 范围 `[画布宽 − right − width, 画布宽 − right]`，Y 范围 `[top, top + height]`。元素只有 X 与 Y 同时相交才越界
- **内容区右边界**：按 token 实算（`right:5.2%` + `width:80px` → 90.6%），禁止硬编码 91.6% 等派生值
- **顶部元素强制约束**：顶部高于 Logo 底边的元素（标题等，Y 已相交），右边界强制 ≤ 画布宽 − right − width；内容主体区（如 `top:28%`）垂直已低于 Logo 底边，不受此约束
- **渲染器从 token 读**：content/cards/timeline/toc 标题块 `right:6%` → `right:calc(l.right + l.width)`，右边界精确对齐 Logo 左边界（1740.2px），缩放等比
- **键名对齐**：全部 `layout.innerPageLogo.size` → `width/height`（数据层唯一真相源）

---

#### 广告法合规审查决策记录（2026-08-20）

**背景：** 用户提出 agent 遵循 skill 生成文字提纲后执行一次中国广告法审查（违禁词/极限词）；命中必须打断生成流程，用户给出修改后继续审查直到流程结束。设计评审时用户纠正两点：① 同义改写建议（如「行业第一」→「行业领先」）同样可能违规——**不代写替代词**，只给固定规则句；② 工作汇报（对内）不审查，仅对外展示场景。

**决策：**
- **范围**：仅对外展示（`scene:"template"` 不查）；工作汇报法律风险低且「行业第一」式表述在内部数据语境常见，不打断用户
- **两层机制**：Agent 层提纲自查（命中 → 打断 → 列出词+位置+原文 → 用户改 → 循环至干净）+ generate.js 代码层精确匹配 fail-loud（exit 1）兜底——LLM 自查判语境但会漏，代码兜底不漏精确词但判不了语境，两层互补
- **词库**：`ad-compliance.json` —— `bannedWords` 极限词+承诺词 ~80 词（词+类别，无改写建议）；`exempt` 代码层豁免清单（`最新版本`/`最佳实践`/`行业第一梯队`/`国家级高新技术企业` 等 263 语境合法词）——代码判不了语境靠 exempt 压误报，agent 层仍按完整词库+语境判断
- **命中处理**：固定规则句「广告法禁止无法核实的绝对化/极限化宣称。建议改为可核实的具体事实表述（数据、案例、认证背书），或直接删除该表述。」——改写方案由用户定（用户才掌握自家事实/数据/认证），agent 只标记和提示方向
- **代码层跳过 custom.html**：含标签/属性，精确子串匹配误报率高，内容层面由 agent 自查兜底

**验证：** node 实测四个样例——S1 对外展示含「行业第一」exit 1（报 slide 0 subtitle）；S2 对外展示干净正常生成；S3 工作汇报 scene=template 含违禁词不检查、正常生成；S4 对外展示含豁免词不误报、正常生成
**状态：** 已实现（2026-08-20），zip 重建 21 文件

---

#### PPTX 生成后审查 + 「编译进 harness」决策记录（2026-08-20）

**背景：** 追加18 的 HTML 侧闸门已落地（generate.js fail-loud），PPTX 侧没有机器闸门——品牌规范对 agent 是声明式。用户定性：品牌规范应像广告法一样做「内部法律」强制。用户追问关键问题：「工作流编号如果 agent 并不是这些节点怎么办」——确认了**叙事层编号拦不住不走节点的 agent**，强制只能来自「检查嵌进产出动作」。

**决策（用户确认：A 必做 + B 并入 + C 双路径）：**
- **强制强度三层（诚实边界，不冒充）：**
  - **generate.js = 真 harness**（HTML 必经渲染路径，检查嵌在产出里，物理绕不过）
  - **4b 代码生成路径 = 交付断言**：`brand-check-pptx.py` 嵌入生成脚本末尾（`not brand_check → sys.exit(1)` 不产出）——agent 照 guide 写代码即无法跳过，最接近「渲染即检查」
  - **设计技能产出路径 = 无机器强制**：产出方式在设计技能手中，只能可选检具 + 清单人工核对——**诚实标注边界**，SKILL/pptx-guide/CLAUDE 三处一致
- **清单必做（A）**：SKILL.md「PPTX 生成后自查（硬门禁）」7 条（颜色白名单读 token / 红底文字=白 / 结尾页最后 / Logo 内嵌等比 / 字体分流 / 图片等比 slogan 9.44 / 封面文字框透明）——声明式兜底，任何路径交付前核对
- **brand-check-pptx.py（C）**：白名单色扫描（对齐 generate.js buildColorWhitelist：colorSchemes 9色 + semantic 子容器 + chartPalette series/muted）/ 红底场景几何相交判定（红底形状边界框 ∩ 文字框 → 文字必须白，两遍扫描）/ 结尾页最后 + 中间「感谢/谢谢」残留 warn / 图片内嵌 / `--external` 禁微软雅黑（latin + ea 双面）
- **红底深档边界定义**：s4-s7（含品牌红 s5）作底触发白字规则；s1-s3 浅档按常规对比度。边界唯一真源在 SKILL.md，`RED_BOTTOM_HEXES` 与之一致

**验证：** 8 样例 + 交付断言 3 路（对外合法开源字体过 / 对内合法微软雅黑过 / 对外违规红底浅粉拦）+ 语义绿合法过（修复后）+ 自造色/ea 面微软雅黑/中间感谢残留全对
**状态：** 已实现（2026-08-20），zip 重建 22 文件

---

#### 自包含化 + 分发方式决策记录（2026-08-20）

**背景：** 后续要维护 skill 并分发给全公司同事（企业版 + 个人版各类 AI 工具）。用户问两个问题：① 更新新版要重装吗？② git clone 能否支持自动更新？经 claude-code-guide 查证官方机制（skill 是运行时目录扫描非安装时复制 → 替换目录/git pull 即更新无需重装；真·自动更新只有 marketplace；加载优先级企业受管 > 用户级 > 项目级），用户确认分发方式为 **zip 为主 + git 作备份**。

**决策：**
- **仓库物理布局不动**：数据文件留在根，git 历史零破坏；仓库根 = 开发/测试工作区，zip = 分发产物（改仓库数据后重建 zip 同步，`测试记录/build-skill-zip.py`）
- **generate.js 改 `__dirname` 相对**：`BASE_DIR` + `resolveBase()`，数据/资产/渲染器从 `__dirname` 解析，任意 CWD 可运行；pages.json/输出 HTML 仍相对调用方 CWD（外部输入，不 resolve）——用户不必 cd 到数据文件旁
- **zip 改自包含 skill 目录结构**：`263group-brand-guidelines/` 为 zip 根，内含 22 文件；解压该目录即完整 skill（企业版/个人版通用）
- **兼容性**：SKILL.md 不用 `${CLAUDE_SKILL_DIR}`（非 Claude Code 工具不识别），用明文路径描述「以 skill 目录为基准」；SKILL.md 裸文件名对 agent 是相对可见范围解析，仓库根和 zip 目录两种布局下均成立，无需改 32 处引用本身
- **更新无需重装**：用户替换 zip 目录或 `git pull`，新会话自动用新版
- **marketplace 暂缓**：改调用名（`/插件名:skill名`）、复制进缓存覆盖用户本地改动、需 plugin.json/marketplace.json 额外配置——等有大量非技术用户、需版本管理时再评估

**验证：** test-generate-anywhere.js（临时目录运行 HTML 含 base64 Logo）+ brand-check 任意 CWD + 打包版解压运行（HTML/PPTX 均自包含生效）
**状态：** 已实现（2026-08-20），zip 0820 重建 22 文件自包含结构

## 待定事项

- 通信蓝色值 — 官方确认后启用（当前暂不可选）
- 云通信 Logo 反白稿
- 位图底图方案 — 豆包生成封面位图 → HTML/PPTX 共用底图 + 动态文字叠加，100% 视觉一致。**已落地为工作汇报红色封面（red-template）**：`scene:template` + `cover.background:red-template`，`assets/cover-red-template.png` 全屏底图 + 左对齐文字
- v2 视口原生方案 — 100vw×100vh + flexbox 替代固定画布+scale。2026-08-11 先以方案A过渡。方案B的视口原生改造待方案A验证稳定后执行。
